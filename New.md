echo "# allworks" >> README.md 
git init
git add README.md 
git commit -m "первая фиксация"
git branch -M основной
источник удаленного добавления git https://github.com/Kuznetsov-dk/allworks.git 
git push -u origin основной